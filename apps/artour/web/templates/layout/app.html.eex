<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title><%= site_title() %> | Admin</title>
    <%= tag :link, rel: "stylesheet", href: static_path(@conn, "/assets/admin.css") %>
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="navigation" class="navbar">
          <%= link site_title(), to: admin_path(@conn, :index), class: "navbar-brand" %>

          <ul class="navbar-nav">
            <li>
              <%= link "Public", to: page_path(@conn, :index) %>
            </li>
            <li>
              <%= link "Posts", to: post_path(@conn, :index) %>
            </li>
            <li>
              <%= link "Images", to: image_path(@conn, :index) %>
            </li>
            <li>
              <%= link "Add post", to: post_path(@conn, :new), class: "btn btn-success btn-sm" %>
            </li>
          </ul>
        </nav>
      </header>

      <p class="alert alert-info" role="alert"><%= Phoenix.Flash.get(@flash, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= Phoenix.Flash.get(@flash, :error) %></p>

      <main class="main-content" role="main">
        <%= @inner_content %>
      </main>

    </div>
    <%= content_tag :script, "", src: static_path(@conn, "/assets/admin.js") %>
  </body>
</html>
