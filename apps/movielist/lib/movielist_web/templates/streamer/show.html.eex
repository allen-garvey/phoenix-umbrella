<%= render(MovielistWeb.SharedView, "show_header.html", 
      conn: @conn, 
      resource_title_plural: "Streamers", 
      resource_path_atom: :streamer_path, 
      item: @streamer, 
      item_name: to_s(@streamer)
    ) 
%>

<ul>

  <li>
    <strong>Name:</strong>
    <%= @streamer.name %>
  </li>

</ul>

<%= if !Enum.empty?(@streamer.movies) do %>
  <table>
    <thead>
      <tr>
        <th>Title</th>
      </tr>
    </thead>
    <tbody>
      <%= for movie <- @streamer.movies do %>
        <tr>
          <td>
            <%= link movie.title, to: Routes.movie_path(@conn, :show, movie) %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
